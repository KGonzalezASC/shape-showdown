export const GAME_WIDTH = 400;
export const GAME_HEIGHT = 600;
export const PADDLE_WIDTH = 80;
export const PADDLE_HEIGHT = 10;
/** Pixels per second while holding left/right (server-simulated). */
export const PADDLE_SPEED = 320;
export const BALL_LOSS_SCORE_PENALTY = 10;
export const SCORE_PER_BUBBLE = 10;
export const SHOOT_COOLDOWN_SEC = 1.5;
/** Duration for score / field floating feedback (seconds). */
export const SCORE_FLOAT_DURATION_SEC = 1;
export const BALL_RADIUS = 8;
export const BUBBLE_RADIUS = 15;
export const BUBBLE_ROWS = 5;
export const BUBBLE_COLS = 8;
export const GAME_DURATION = 120; // 2 minutes

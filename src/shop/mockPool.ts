/** @deprecated Prefer `./shopCatalog`. Kept so existing imports keep working during the rename. */
export {
  SHOP_CATALOG,
  SHOP_ROLL_POOL,
  SHOP_ITEM_BY_ID,
  resolveShopOffers,
  SHOP_MOCK_POOL,
} from './shopCatalog';
